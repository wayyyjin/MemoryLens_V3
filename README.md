# Memory Lens

AI Photo-based Life Logging Agent.

## Streamlit Secrets

Add this in Streamlit Cloud > Advanced settings > Secrets:

```toml
OPENAI_API_KEY="sk-proj-..."
```

## Run locally

```bash
pip install -r requirements.txt
python -m streamlit run app.py
```
